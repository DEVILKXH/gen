package com.${packageName}.${modelName}.mapper;

import com.${packageName}.${modelName}.entity.${className};
import com.core.extend.base.mapper.BaseMapper;

public interface ${className}Mapper extends BaseMapper<${className}>{
	
}

